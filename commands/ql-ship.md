---
name: ql:ship
description: "[已更名] 此命令已更名为 /ql-deliver(交付:推送 PR + 章节留档)。请使用新命令。"
argument-hint: ""
allowed-tools: []
---

⚠️ **此命令已更名为 `/ql-deliver`**(0.7.0 起,名称直白化)。

请改用:

```bash
/ql-deliver
```

功能不变:前置检查(验证 + 评审)→ 确认收尾方式 → PR + 章节留档。
